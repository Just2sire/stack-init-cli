<?php
{{#if laravelOptions.use_strict_types}}

declare(strict_types=1);
{{/if}}

namespace App\Listeners;

use App\Events\{{name}}{{eventSuffix}};
use Illuminate\Contracts\Queue\ShouldQueue;
use Illuminate\Queue\InteractsWithQueue;

class Log{{name}}{{eventSuffix}}Listener implements ShouldQueue
{
    use InteractsWithQueue;

    public function handle({{name}}{{eventSuffix}} $event): void
    {
        logger()->info('{{name}} {{eventSuffix}}', ['id' => $event->{{varName}}->getKey()]);
    }
}
