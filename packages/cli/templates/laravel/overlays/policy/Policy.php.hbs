<?php
{{#if laravelOptions.use_strict_types}}

declare(strict_types=1);
{{/if}}

namespace App\Policies;

{{#unless isUserModel}}
use App\Models\User;
{{/unless}}
use App\Models\\{{name}};
use Illuminate\Auth\Access\HandlesAuthorization;

class {{name}}Policy
{
    use HandlesAuthorization;

    public function viewAny(User $user): bool { return true; }

    public function view(User $user, {{name}} ${{modelParamName}}): bool { return true; }

    public function create(User $user): bool { return true; }

    public function update(User $user, {{name}} ${{modelParamName}}): bool { return true; }

    public function delete(User $user, {{name}} ${{modelParamName}}): bool { return true; }
{{#if generate.softDelete}}

    public function restore(User $user, {{name}} ${{modelParamName}}): bool { return true; }

    public function forceDelete(User $user, {{name}} ${{modelParamName}}): bool { return false; }
{{/if}}
}
