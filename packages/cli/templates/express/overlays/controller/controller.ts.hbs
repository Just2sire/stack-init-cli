import { Request, Response } from 'express';
import { {{pascalName}}Service } from '../services/{{name}}.service';

export class {{pascalName}}Controller {
  private service: {{pascalName}}Service;

  constructor() {
    this.service = new {{pascalName}}Service();
  }

  getAll = async (req: Request, res: Response) => {
    try {
      const data = await this.service.getAll{{pluralName}}();
      res.json(data);
    } catch (error: any) {
      res.status(500).json({ error: error.message });
    }
  };

  getById = async (req: Request, res: Response) => {
    try {
      const id = {{pkExtraction}};
      const data = await this.service.get{{pascalName}}ById(id);
      if (!data) return res.status(404).json({ message: 'Not found' });
      res.json(data);
    } catch (error: any) {
      res.status(500).json({ error: error.message });
    }
  };

  create = async (req: Request, res: Response) => {
    try {
      const data = await this.service.create{{pascalName}}(req.body);
      res.status(201).json(data);
    } catch (error: any) {
      res.status(400).json({ error: error.message });
    }
  };

  update = async (req: Request, res: Response) => {
    try {
      const id = {{pkExtraction}};
      const data = await this.service.update{{pascalName}}(id, req.body);
      res.json(data);
    } catch (error: any) {
      res.status(400).json({ error: error.message });
    }
  };

  delete = async (req: Request, res: Response) => {
    try {
      const id = {{pkExtraction}};
      await this.service.delete{{pascalName}}(id);
      res.status(204).send();
    } catch (error: any) {
      res.status(500).json({ error: error.message });
    }
  };
}
